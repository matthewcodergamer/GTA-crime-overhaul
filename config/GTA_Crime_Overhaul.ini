; GTA Crime Overhaul
; Story Mode / offline only.

[Runtime]
Enabled=true
DebugLogging=true
DebugOverlay=false
DebugHotkeys=true

[Systems]
WitnessSystem=true
PersistentCases=true
VehicleIdentity=true
RobberySystem=true

[Performance]
; Core scheduler lanes are frame, 5 Hz, 2 Hz and 1 Hz as documented in ARCHITECTURE.md.
; Feature-specific range/timing values are added only when their owning systems are implemented.

[Compatibility]
; Runtime mission compatibility uses Normal / Restricted / Suspended gating.
; Per-mission/per-interior overrides may be added here when validated.
